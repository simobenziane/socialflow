# .env.vps.example - VPS Hybrid Configuration with Tailscale
# Copy this to .env on your VPS: cp .env.vps.example .env

# ===========================================
# SocialFlow VPS Configuration
# ===========================================

# Ports (bind to localhost, nginx proxies externally)
UI_PORT=3000
N8N_PORT=5678

# API Configuration - Use your actual domain
VITE_API_BASE=https://your-domain.com/webhook
WEBHOOK_URL=https://your-domain.com

# n8n Configuration
N8N_PROTOCOL=https
TZ=Europe/Berlin

# ===========================================
# DATA PATH - VPS
# ===========================================
DATA_PATH=/opt/socialflow-data

# ===========================================
# OLLAMA - Via Tailscale
# ===========================================
# Set this to your LOCAL machine's Tailscale IP
# Find it with: tailscale ip -4 (on your local machine)
#
# Example: If your local machine's Tailscale IP is 100.100.100.2
OLLAMA_HOST=http://100.x.x.x:11434

# ===========================================
# IMPORTANT: Tailscale Setup
# ===========================================
# 1. Install Tailscale on VPS:     curl -fsSL https://tailscale.com/install.sh | sh
# 2. Install Tailscale on local:   brew install --cask tailscale (Mac) or download (Windows)
# 3. Join same Tailscale network on both machines
# 4. Get local machine's Tailscale IP: tailscale ip -4
# 5. Set OLLAMA_HOST above to that IP
# 6. Ensure Ollama binds to all interfaces on local machine:
#    - Mac/Linux: OLLAMA_HOST=0.0.0.0:11434 ollama serve
#    - Windows: Set OLLAMA_HOST=0.0.0.0:11434 environment variable
